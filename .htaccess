Options -Indexes

# Prevent access to config files
<FilesMatch "^(db\.php|.*\.sql)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# PHP settings
<IfModule mod_php.c>
    php_value upload_max_filesize 5M
    php_value post_max_size 8M
    php_value max_execution_time 60
</IfModule>
